// O valor inicial de i é 1. Enquanto i for menor ou igual a 10, 
// multiplica i pelo número (no caso, 5) e exibe o resultado.
for (let i = 1; i <= 10; i++) {
    console.log(i * 5);
}
/////////////////////////////////////////////////////////////////////
//PODERIA FAZER ASSIM TAMBEM:

const numero = 5;
for (let i = 1; i <= 10; i++) {
    console.log(i * numero);
}